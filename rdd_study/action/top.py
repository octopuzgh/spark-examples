# rdd.top(num)                 # 取最大的 num 个元素（默认排序）
# rdd.top(num, key=func)       # 按 key 函数取最大的 num 个

# stream.sorted(comparator.reversed())
#       .limit(num)
#       .collect(Collectors.toList())
